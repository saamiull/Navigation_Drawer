<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="org.samiulhaq.navigationdrawer">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NavigationDrawer">
        <activity android:name=".Home"></activity>
        <activity android:name=".ReturnActivity" />
        <activity android:name=".LaptopActivity" />
        <activity android:name=".VoiceActivity" />
        <activity android:name=".ChromeReaderActivity" />
        <activity android:name=".ReadMoreActivity" />
        <activity android:name=".AddCommentActivity" />
        <activity android:name=".InsertCommentActivity" />
        <activity android:name=".BookActivity" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/Theme.AppCompat.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>